2.3 a variable (defvar) is routinely changed during the course of running the program. However, a parameter will normally stay constant.
a change to a parameter is considered a change to the program, while a variable is to be changed by the program.
